import styled from 'styled-components';
import {Container} from "../../common.styles";

const UserDetailPageWrapper = styled.div`
  position: relative;
  width: 100%;
  margin: 32px 0;
`;

const UserDetailPageContainer = styled(Container)`
`;

export {
    UserDetailPageWrapper,
    UserDetailPageContainer
};